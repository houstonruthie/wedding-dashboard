import { PlaceholderPage } from "../components/PlaceholderPage";

export default function GuestPreviewPage() {
  return (
    <PlaceholderPage
      eyebrow="Guest Site Preview"
      title="A staging area for the public site."
      description="Use this as the draft space for guest-facing pages before anything becomes official: story, schedule, travel, registry, RSVP, and gallery."
    />
  );
}
